examiner:
  exam-data-path: testdata
  locale: ru-RU
spring:
  main:
    allow-bean-definition-overriding: true
    allow-circular-references: true
  output:
    ansi:
      enabled: always
  shell:
    interactive:
      enabled: false